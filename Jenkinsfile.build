pipeline {
    agent any
    stages {
        stage("build") {
            steps {
                sh 'docker build . -t ivan-git-ai/django_demo_site:latest -t ivan-git-ai/django_demo_site:${GIT_COMMIT} '
            }
        }
    }
}
