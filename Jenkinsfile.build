pipeline {
    agent any
    stages {
        stage("build") {
            steps {
                sh 'docker build . -t ivan-git-ai/django_demo:latest -t ivan-git-ai/django_demo:${GIT_COMMIT} '
            }
        }
    }
}
