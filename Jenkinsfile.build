pipeline {
    agent any
    stages {
        stage("build") {
            steps {
                sh 'docker build . -t ivan-git-ai/django_build:latest -t ivan-git-ai/django_build:${GIT_COMMIT} '
            }
        }
    }
}
